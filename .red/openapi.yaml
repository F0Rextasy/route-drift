openapi: 3.0.0
info:
  title: Demo API
  version: 1.0.0
paths:
  /users:
    get:
      summary: List users
    post:
      summary: Create a user
  /users/{id}:
    get:
      summary: Get one user
    delete:
      summary: Delete a user
  /health:
    get:
      summary: Health check
  /legacy:
    get:
      summary: Old endpoint nobody implemented
  /orders/{id}:
    get:
      summary: Get one order
